#include<stdio.h>
#include<math.h>//need to include to use math functions
int main(){
    double a = sqrt(3);
    double b= pow(5,2);
    int c= round(3.14);
    int d = ceil(3.14);
    int e = floor(3.14);
    double f= fabs(-100);
    double g= log(3);
    double h= sin(45);
    double i= cos(45);
    double j= tan(45);
    

    return 0;
}